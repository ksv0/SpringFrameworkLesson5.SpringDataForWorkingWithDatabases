# Используйте официальный легкий образ Java.
FROM openjdk:11-jre-slim

# Установите рабочую директорию в контейнере как /app
WORKDIR /app

# Скопируйте JAR-файл вашего приложения из локальной машины в контейнер в каталог /app
COPY target/*.jar app.jar

# Запустите приложение при запуске контейнера
ENTRYPOINT ["java", "-jar", "app.jar"]
